// 小程序入口。
// Phase 1 仅保留生命周期骨架：云环境初始化（wx.cloud.init）、登录态恢复等在 Phase 2 接入，
// 具体时机与顺序需等待 Design Bible 相关章节补齐后确认。
App({
  onLaunch() {
    // Phase 2: wx.cloud.init({ env: 环境ID }) —— 见 docs/decisions/pending-decisions.md
  },
});
